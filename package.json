{
  "name": "practice",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "live-server --host=localhost --port=3010 --entry-file=./index.html --quiet  --mount=/:./src",
    "build": "gulp build --api=https://fluidci-development.api.web.objectsystems.com && npm run servebuild"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.1"
  }
}
